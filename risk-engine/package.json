{
  "name": "risk-engine",
  "version": "0.1.0",
  "private": true,
  "description": "OWASP Risk Rating engine for the transacciones-api DevSecOps pipeline: normalizes SAST/SCA/Secret Scanning findings, enriches them with application context, computes risk, and applies policy decisions.",
  "bin": {
    "risk-engine": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^20.14.15",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
